---
description: 新增功能后必须同步更新 README.md
alwaysApply: true
---

# 功能变更后自动更新 README

每次为本项目**新增、修改或移除用户可见功能**时，必须在同一轮改动中同步更新 `README.md`，不要等用户再提醒。

## 何时必须更新

- 新增交互（编辑、置顶、标签、快捷键等）
- 改变数据字段 / localStorage 结构
- 改变开发或打包方式（如 `npm start` 行为）
- 修复会影响使用说明的行为（如菜单遮挡、排序规则）

纯内部重构、样式微调且不影响文档描述时，可不改 README。

## 需要同步的章节（按需）

1. **开篇简介**与 **功能亮点**表格
2. **模块职责 / 数据存储 / 流程**（若架构或字段变了）
3. **操作与快捷键**
4. **关键可调参数**（若新增常量）
5. **常见问题**（若有新坑）
6. **版本历史**：追加一条，日期用当天；小功能升补丁/次版本说明即可

## 写法要求

- 与代码实际行为一致，不写未实现功能
- 保持现有中文文档风格与表格结构
- 简洁：亮点用一行说明，细节放架构/快捷键章节

## 完成标准

功能相关代码改完后，检查 README 是否已反映本次变更；若未反映，先改 README 再结束回复。
